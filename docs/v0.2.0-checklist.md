# v0.2.0 Checklist (FR/TG mapped)

## Scope
Focus for v0.2.0:
- Exercise CRUD + permissions
- Token scope hardening (trainer/admin athlete scope)
- Session reliability autosave/recovery
- SQLite backup/restore guardrails
- Core workflow confidence tests

## Checklist

### Exercise CRUD (FR-5, FR-6, FR-32)
- [x] `GET /v1/exercises/`
- [x] `POST /v1/exercises/`
- [x] `PATCH /v1/exercises/{exercise_id}`
- [x] `DELETE /v1/exercises/{exercise_id}`
- [x] Global shared exercise pool behavior documented/implemented (no per-user exercise library)
- [x] Integration tests for create/update/delete + forbidden paths

### Token scope hardening (FR-33, FR-3, FR-4)
- [x] Login supports optional `athlete_ids` token scope for trainer/admin
- [x] Scope enforced in permission checks (`token_scope_forbidden`)
- [x] Reject unassigned trainer scope requests
- [x] Integration tests for scoped-token allow/deny paths

### Session reliability (FR-23, FR-18..22)
- [x] `workout_sessions.last_saved_at`
- [x] `POST /v1/sessions/{session_id}/autosave`
- [x] Set logging updates save timestamp
- [x] In-progress resume path validated by tests
- [x] Web background autosave in active session
- [x] `workout_sessions.version` + `updated_at` for optimistic concurrency
- [x] Stale-write protection on set log / autosave / finish
- [x] Duplicate in-progress session start protection
- [x] Local active-session backup + recovery in web UI
- [x] Autosave triggers on interval + notes changes + tab hide / page hide
- [x] Autosave tuned to be event-driven first with slower periodic backup and reduced no-op version churn
- [x] Offline pending set replay refreshes latest session version before retry

### Web exercise picker UX (NFR-1)
- [x] Template editor uses fuzzy text suggestions
- [x] Inline create-new exercise action from picker
- [x] Save guard for unresolved exercise rows

### SQLite operational guardrails (TG-8, TG-4)
- [x] WAL mode + busy timeout in DB connection setup
- [x] Backup script (`infra/backup/backup.sh`) using SQLite online backup API
- [x] Restore script (`infra/backup/restore.sh`) with integrity check
- [x] Deployment/admin runbook (`docs/admin/deployment.md`)

### Testing and quality (TG-9..14, TG-21)
- [x] API tests passing
- [x] Lint/typecheck passing
- [x] Added core end-to-end integration workflow test
- [x] Added browser E2E (Playwright) happy-path workflow
- [x] Updated E2E coverage to match Dashboard / Programs / Train navigation and dashboard-first scheduling flow

### Recent UX / ops polish
- [x] Train page hierarchy updated to prioritize resume / today’s workout before secondary start flows
- [x] Autosave surfaced as passive in-session status instead of a floating Train-page action row
- [x] Mobile navigation updated to a sticky section top bar plus left-side drawer toggle
- [x] Calendar header alignment and month-view day-cell sizing polished for better scanning
- [x] Runtime backups moved outside the repo to `~/backups/workout-app`

## Deferred / next
- Mobile app milestone (platform requirement 4.1) still pending.
- Consider replacing manual `alembic stamp ...` deploy step with a cleaner first-class migration/bootstrap strategy for legacy SQLite installs.
